import { Button } from 'react-bootstrap'

// Samo UI element — generisanje i preuzimanje pravog CSV fajla dolazi u Week 4.
function CsvIzvozDugme() {
  return (
    <Button variant="outline-secondary" size="sm" type="button">
      Izvezi CSV
    </Button>
  )
}

export default CsvIzvozDugme
